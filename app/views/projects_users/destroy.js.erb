<% if @projects_user.errors.blank? %>
  $('#projects_user_<%= @projects_user.id %>').remove();
  if ($("#projects_users_datatable_body tr").length == 0) {
    $('#users_datatable_space').html("<%= escape_javascript (render partial: 'shared/no_resource_present', locals: { record: 'users.label' }) %>");
  }  
<% end %>
$('#flash_messages').html("<%= escape_javascript render partial: 'shared/flash_messages', flash: flash %>");
