$('#nav-users').html("<%= escape_javascript render('users_index', project: @project, projects_users: @projects_users) %>");
<% if @projects_users.present? %>
  $('#users_datatable_space').html("<%= escape_javascript render('user_datatable', projects_users: @projects_users, project: @project) %>");
<% else %>
  $('#users_datatable_space').html("<%= escape_javascript (render partial: 'shared/no_resource_present', locals: { record: t('users.label') }) %>");
<% end %>
