$("#modal").html("<%= escape_javascript render('form', project: @issue.project, issue: @issue, projects: @projects, users: @users )%>");
$("#modal").modal('show');

var dateFunc = function() {
  $(".js-flatpickr-date").flatpickr({
    enableTime: false
  });
}
dateFunc();
