$("#modal").html("<%= escape_javascript render('form', project: @project, issue: @issue, projects: @projects, users: @users )%>");
$("#modal").modal('show');

var dateFunc = function() {
  $(".js-flatpickr-date").flatpickr({
    enableTime: false
  });
}
dateFunc();

<% if(@project.nil? ) %>
{
  $('#issue_project_id').val(null);
}
<% end %>
