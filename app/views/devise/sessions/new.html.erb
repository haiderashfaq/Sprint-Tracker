
<% if PUBLIC_SUBDOMAINS.include? request.subdomain %>
  <%= render partial: "find_by_email" %>
  
<% else %>
  <%= render partial: "form", locals: { resource: resource, email:params[:email] } %> 
<% end %>

                               

