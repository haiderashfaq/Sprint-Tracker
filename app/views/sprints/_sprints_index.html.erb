<% sprint_path_value = local_assigns[:project] ? new_project_sprint_path : new_sprint_path %>
<% if can? :create, project => Sprint %>
  <div class="container mt-3">
    <div class="row d-flex justify-content-end">
      <div class="col-2">
        <%= link_to t('shared.create_new_resource', resource_label: t('sprints.label')), sprint_path_value, remote: true, class:"btn btn-primary w-100 bi bi-plus" %>
      </div>
    </div>
  </div>
<% end %>
<div class="container mt-3">
  <div class="text-center" id="sprints_datatable_space"></div>
</div>