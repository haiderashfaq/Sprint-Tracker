<% if @sprint.errors.blank? %>
  $.get(window.location.origin + window.location.pathname + "/sprints.js");
  $('#nav-sprints-tab').tab('show');
<% end %>
$('#flash_messages').html("<%= escape_javascript render partial: 'shared/flash_messages', flash: flash %>");
